import { StyleSheet } from "react-native";

export const PADDING = 20;
export const SEPARATOR_PADDING = 7;
export const BORDER_RADIUS = 5;
export const MARGIN = 10;
export const FONT_SIZE_P = 20;

export default StyleSheet.create({
  h1: {
    fontSize: 22,
    fontWeight: "800"
  },
  h2: {
    fontSize: 18
  }
});
